"""Per-model EXP-24 DPBR entrypoints.

HF ids and Kaggle pip profiles live in `_gen_model_files.py`. Reference notebooks
(Phi/Llama3.2, Qwen3.5 Vision, Gemma-4) use `ref_*` profiles with pinned
transformers; use a **fresh Kaggle session** per profile family to avoid conflicts.
"""
